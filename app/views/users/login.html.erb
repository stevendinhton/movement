<%= form_for @user, url: {controller: "users", action: "validate_login"} do |f| %>
  <%= f.label :username, "User Name:" %>
  <%= f.text_field :username %>
  <%= f.label :username, "Password:" %>
  <%= f.password_field :password %>
  <%= f.submit "Login" %>
<% end %>
